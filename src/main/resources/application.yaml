spring:
  application:
    name: places-been
  datasource:
    url: jdbc:postgresql://localhost:5433/places_been_db
    username: admin
    password: admin
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true